#############################################################
# Django / Project
#############################################################
# True / False (acepta: true,1,yes / false,0,no)
DEBUG=True
# Mover la SECRET_KEY fuera del código fuente (añade una distinta en producción)
SECRET_KEY=changeme-coloca-una-clave-larga-y-segura

#############################################################
# URLs Base
#############################################################
# Debe terminar con '/'
BASE_URL=http://localhost:8000/
BASE_URL_FRONTEND=http://localhost:5173/

#############################################################
# Base de Datos MySQL
#############################################################
DATABASE_SERVER=localhost
DATABASE_PORT=3306
DATABASE_USER=root
DATABASE_PASSWORD=tu_password
DATABASE_BD=django_db

#############################################################
# SMTP (Ejemplo Mailtrap Sandbox)
#############################################################
# Host (según tu captura): sandbox.smtp.mailtrap.io
SMTP_SERVER=sandbox.smtp.mailtrap.io
# Puedes usar 25, 465, 587 o 2525. Recomendado 587 o 2525 con STARTTLS
SMTP_PORT=587
SMTP_USER=d3350bf83951c7
# Coloca aquí tu password real (no lo compartas públicamente)
SMTP_PASSWORD=TU_PASSWORD_MAILTRAP

# Notas:
# - Si cambias el puerto a 465 necesitarías conexión SSL directa (no STARTTLS).
# - En 587 o 2525 el código activará STARTTLS.
# - Asegúrate de no commitear este archivo (.env) al repositorio.
